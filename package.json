{
  "name": "@dylan2045ad/dyl-nawful",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate": "node --check app.js && node --check scripts/build-snapshot.mjs && node --check scripts/collect-x-posts.mjs",
    "collect": "node scripts/collect-x-posts.mjs",
    "refresh": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/refresh-and-publish.ps1"
  },
  "dependencies": {
    "playwright-core": "1.62.1"
  },
  "engines": {
    "node": ">=20"
  }
}
